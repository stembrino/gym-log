import type { GymLogBackupFile } from "@/features/data-transfer/types/backup";

export async function restoreBackupReplace(_backup: GymLogBackupFile) {
  throw new Error("restoreBackupReplace ainda não implementado.");
}
